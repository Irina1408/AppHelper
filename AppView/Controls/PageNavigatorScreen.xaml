<UserControl x:Class="AppView.Controls.PageNavigatorScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Margin="10">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label x:Name="pageSpecificHeader"/>
        <DockPanel Grid.Row="1">
            <StackPanel x:Name="pageButtonsPanel" DockPanel.Dock="Top" Orientation="Horizontal"></StackPanel>
            <Frame x:Name="pageNavigationFrame" NavigationUIVisibility="Hidden"></Frame>
        </DockPanel>
    </Grid>
</UserControl>
